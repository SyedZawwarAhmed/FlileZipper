#include <iostream>
#include <string>
#include <cstring>

using namespace std;

struct Character {
    char character;
    int frequency;
};

Character *getFrequencies(char charArray[]) {
    
    size_t len = strlen(charArray);
    cout << charArray << endl;
}